---
title: "3.7 — Sustentação a partir da distribuição de pressões"
categories: [UE3, OE3.26-B2, OE3.27-B2, OE3.28-B3, OE3.29-B4]
---

<!-- ═══ OBJETIVOS DESTE TÓPICO — fonte: _docs/FIS_2_RA.md (não editar) ═══
OE 3.26 [B2] Explicar o conceito de sustentação e sua importância na aerodinâmica
            → naval: Sustentação do leme; geometria do hélice
OE 3.27 [B2] Relacionar a distribuição de pressões ao redor do corpo com a força de sustentação
            → naval: Lado de pressão e sucção da pá do hélice
OE 3.28 [B3] Aplicar a equação da sustentação para perfis aerodinâmicos
            → naval: Perfil da pá e geração de empuxo propulsivo
OE 3.29 [B4] Comparar a sustentação em diferentes condições de escoamento
            → naval: Hélice de passo fixo vs. passo controlável
═══════════════════════════════════════════════════════════════════════ -->

{{< aula 29-30 >}}

Enquanto o arrasto resiste o movimento (força contrária à velocidade), a **sustentação** é uma força perpendicular ao escoamento que faz trabalho útil. Uma asa de avião gera sustentação para voar; uma pá de hélice gera sustentação para empurrar o navio; um leme gera sustentação para governar. Este tópico explora como a **distribuição de pressões** ao redor de um corpo imerso cria sustentação e como a forma do perfil (asimetria, ângulo de ataque) governa a sua magnitude. Compreender sustentação é compreender por que a hélice funciona e como o leme muda a proa.

## Conceito de sustentação {{< ra OE3.26 B2 >}}

A **sustentação** é a componente de força perpendicular ao escoamento (e perpendicular também ao arrasto). Nasce de uma **assimetria** na distribuição de pressões ao redor do corpo. Um cilindro redondo imerso em fluxo simétrico gera pressões iguais na frente e atrás — sem sustentação. Mas um **perfil aerodinâmico** (uma forma alongada, curva de um lado) gera pressão **menor** de um lado (lado de sucção) e **maior** do outro (lado de pressão), resultando em força perpendicular.

Para um perfil de asa ou pá de hélice:

$$L = \frac{1}{2} \rho v^2 A C_L$$

onde:
- $L$ é a sustentação (N)
- $\rho$ é a densidade do fluido
- $v$ é a velocidade do escoamento relativo
- $A$ é a área de referência (asa / pá)
- $C_L$ é o **coeficiente de sustentação** (adimensional), que depende da forma do perfil e do **ângulo de ataque**.

## Distribuição de pressões e geração de sustentação {{< ra OE3.27 B2 >}}

Quando um fluido escoa ao redor de um perfil, a geometria **desvia** as linhas de corrente. Pela continuidade, onde as linhas se aproximam (região de convergência), a velocidade aumenta e, por Bernoulli, a pressão cai. Onde as linhas se afastam (região de divergência), a velocidade cai e a pressão sobe.

Um perfil aerodinâmico típico (asa):
- **Lado de sucção (dorso):** curvatura maior, escoamento converge, velocidade alta, **pressão baixa**.
- **Lado de pressão (ventre):** curvatura menor, escoamento diverge, velocidade baixa, **pressão alta**.

A **diferença de pressão** entre os dois lados integrada sobre a área gera sustentação vertical.

Para uma pá de hélice:
- O lado de sucção (dorso da pá) tem pressão mais baixa.
- O lado de pressão (ventre da pá) tem pressão mais alta.
- A diferença de pressão puxa a água para trás, empurrando o navio para frente (reação).

O coeficiente $C_L$ típico para um perfil bem-projetado varia com o ângulo de ataque $\alpha$:
- A baixo $\alpha$ (0–10°), $C_L$ cresce linearmente.
- Próximo a $\alpha \approx 15-20°$, $C_L$ atinge máximo.
- Além disso, **stall** (perda de sustentação) ocorre; separação do escoamento destrói a sustentação.

## Equação da sustentação em perfis aerodinâmicos {{< ra OE3.28 B3 >}}

Para um perfil de pá de hélice ou de leme operando em regime normal:

$$L = \frac{1}{2} \rho v^2 A C_L$$

O coeficiente $C_L$ é determinado experimentalmente (tabelas de perfis NACA, e.g. NACA 63A517) ou computacionalmente. Típicos valores:
- $C_L = 0.5$ para $\alpha = 5°$
- $C_L = 1.0$ para $\alpha = 10°$
- $C_L = 1.5$ para $\alpha = 15°$ (aproximação do stall)

A **força total** sobre a pá é a soma de sustentação e arrasto:

$$F_{\text{total}} = \sqrt{L^2 + D^2}$$

onde $D$ é o arrasto da pá (calculado de forma similar: $D = \frac{1}{2} \rho v^2 A C_D$).

Para uma **hélice propulsiva**, o objetivo é maximizar a razão $L/D$ (eficiência) — gerar muita sustentação com pouco arrasto. Isso é alcançado com ângulos de ataque moderados (5–15°) e perfis bem-escolhidos.

## Sustentação em diferentes regimes de escoamento {{< ra OE3.29 B4 >}}

A sustentação depende de como o perfil está imerso no escoamento:

**Número de Reynolds baixo (Re < 10⁴):**
Escoamento laminar prevalece; transição entre sustentação linear e separação é gradual. Perfis delgados funcionam bem.

**Número de Reynolds alto (Re > 10⁶):**
Escoamento turbulento; camada limite turbulenta oferece resistência maior; mas sustentação está bem-estabelecida. Perfis espessos (robustos) funcionam bem. Caso da maioria dos navios.

**Ângulo de ataque variável:**
Uma pá de hélice de **passo fixo** funciona em um ângulo de ataque projetado. Se a velocidade do navio muda drasticamente, o ângulo efetivo muda e a eficiência cai.

Uma pá de **passo variável (controlável)** pode girar em torno de seu eixo, alterando o ângulo. Isso permite otimizar a eficiência em diferentes velocidades — crítico para navios que operam em larga faixa de velocidades (ex.: navios de pesquisa, embarcações de trabalho offshore).

**Cavitação:**
A uma velocidade muito alta, a pressão no lado de sucção pode cair abaixo da pressão de vapor da água. Bolhas se formam (cavitação), destruindo a sustentação e danificando a pá. Hélices navais são projetados com margem para evitar cavitação até velocidades máximas esperadas.

::: {.sp}
Uma pá de hélice de navio graneleiro tem área aproximada $A = 12$ m² e opera em água salgada ($\rho = 1025$ kg/m³) a uma velocidade relativa (navio + rotação) $v = 8$ m/s. O coeficiente de sustentação do perfil é $C_L = 0.8$ e o coeficiente de arrasto é $C_D = 0.04$. Calcule:
(a) A sustentação e arrasto da pá em uma rotação (suponha mudança negligenciável de $v$ durante a rotação).
(b) A potência requerida para gerar essa sustentação (negligenciando perdas).
(c) Como a sustentação muda se a velocidade aumenta de 8 para 10 m/s.
(d) Se o ângulo de ataque aumentar em 5° (aumentando $C_L$ para 1.0 e $C_D$ para 0.06), qual é o novo ponto de operação?

**(a) Modelagem.** Sustentação e arrasto da pá: $L = \tfrac{1}{2}\rho v^2 A\,C_L$,
$D = \tfrac{1}{2}\rho v^2 A\,C_D$. A sustentação é perpendicular ao escoamento e não
realiza trabalho; o custo em potência vem do arrasto, $P = D\,v$. Como $L \propto v^2$,
a razão de sustentações entre duas velocidades é $(v_2/v_1)^2$.

**(b) Desenvolvimento.** Pressão dinâmica $q = \tfrac{1}{2}\rho v^2 = \tfrac{1}{2}(1025)(64) = 32\,800\ \text{Pa}$.
$$L = q\,A\,C_L = 32\,800 \times 12 \times 0{,}8 \approx 3{,}15{\times}10^5\ \text{N} = 315\ \text{kN}$$
$$D = q\,A\,C_D = 32\,800 \times 12 \times 0{,}04 \approx 1{,}57{\times}10^4\ \text{N} = 15{,}7\ \text{kN}$$
$$P_{\text{arrasto}} = D\,v = 15\,744 \times 8 \approx 126\ \text{kW}$$

Aumentando $v$ para 10 m/s: $L' = 315 \times (10/8)^2 \approx 492\ \text{kN}$.
Com $\alpha + 5°$ ($C_L = 1{,}0$, $C_D = 0{,}06$, $v = 8$): $L = 394\ \text{kN}$, $D = 23{,}6\ \text{kN}$.

**(c) Resultado.** A 8 m/s: $L \approx 315\ \text{kN}$, $D \approx 15{,}7\ \text{kN}$,
custo $\approx 126\ \text{kW}$. A 10 m/s a sustentação salta para $\approx 492\ \text{kN}$
(fator 1,56). Aumentando o ângulo de ataque: mais sustentação (394 kN), mas a
eficiência $L/D$ cai de 20 para 17.

**(d) Análise crítica.** A dependência $L \propto v^2$ é a mesma do leme (por isso se dá
máquina adiante ao manobrar a baixa velocidade). Aumentar o ângulo de ataque rende mais
empuxo, mas com queda de $L/D$ — e, perto do estol ou em alta velocidade, o lado de
sucção pode cair abaixo da pressão de vapor e cavitar, destruindo a sustentação: por
isso o projeto do hélice guarda margem. Note que $P = D\,v$ é só o custo do arrasto da
pá; a potência propulsiva útil vem do empuxo, tratado por Kutta–Joukowski (tópico 3.8).
:::

::: {.callout-warning}
## Erros clássicos

- **Sustentação sempre perpendicular ao escoamento?** Em teoria, sim; na prática, há uma pequena componente paralela (componente de arrasto que depende de $\alpha$). Não são perfeitamente desacopladas.
- **Confundir ângulo de ataque com ângulo de passo:** ângulo de passo é a inclinação geométrica da pá; ângulo de ataque é o ângulo entre a corda da pá e a velocidade do escoamento relativo. Diferem se há velocidade rotacional.
- **Stall é cataclismático:** não é um evento instantâneo, mas transição gradual. Além disso, asa-delta (delta wing) e alguns perfis especiais têm "stall suave" onde sustentação degrada lentamente.
:::

## Veja a sustentação crescer e o estol chegar

A pá (ou o leme) inclina-se em relação ao escoamento pelo ângulo de ataque $\alpha$. O
coeficiente $C_L$ cresce quase linearmente com $\alpha$ até que, perto de $15$–$18°$, o
escoamento se descola do dorso e a sustentação **despenca** — o estol. Ajuste $\alpha$ e
a velocidade e acompanhe $L$, $D$ e a razão $L/D$.

```{python}
#| fig-cap: "Coeficiente de sustentação de um perfil em função do ângulo de ataque: linear (C_L ≈ 0,11·α por grau) até o estol em ~16°, depois queda abrupta. Ao lado, o perfil inclinado com o lado de sucção (baixa pressão) e de pressão. Mesmos parâmetros da versão interativa."
#| echo: false
# Versão estática (paridade PDF) — mesmos parâmetros da versão interativa (p5.js)
import numpy as np
import matplotlib.pyplot as plt

a = np.linspace(-4, 22, 300)
def CL(al):
    lin = 0.11*al
    return np.where(al <= 16, lin, np.maximum(0.3, 1.76 - 0.14*(al-16)))
fig, ax = plt.subplots(figsize=(9, 3.4))
ax.plot(a, CL(a), color='#1F47C8', lw=2)
ax.axvline(16, ls='--', color='#C81F1F', lw=1.2)
ax.annotate('estol', xy=(16, 1.2), xytext=(17, 1.2), color='#C81F1F', fontsize=9)
ax.axhline(0, lw=0.5, color='0.7')
ax.set_xlabel('ângulo de ataque α (graus)'); ax.set_ylabel('$C_L$')
plt.tight_layout(); plt.show()
```

```{=html}
<!-- Versão interativa — p5.js (tabela Q4: MOVIMENTO — o perfil inclinando e o escoamento
     em torno). p5 de libs/ (offline, Q6). Mesmos parâmetros da matplotlib:
     C_L linear 0,11/grau até estol em 16°; velocidade e α ajustáveis. -->
<script src="../../libs/p5.min.js"></script>
<style>
  .su-wrap{max-width:1000px;margin:1rem auto;}
  .su-wrap .su-row{display:flex;flex-wrap:wrap;gap:8px 20px;align-items:center;margin-bottom:8px;}
  .su-wrap .sld{display:flex;flex-direction:column;gap:2px;min-width:150px;flex:1 1 150px;}
  .su-wrap .sld label{font-weight:600;font-size:12.5px;}
  .su-wrap .sld .val{color:#c85c2e;font-weight:700;}
  .su-wrap input[type=range]{width:100%;max-width:220px;accent-color:#c85c2e;}
  .su-wrap button{border:1px solid #c9c2b6;background:#fff;border-radius:8px;padding:5px 12px;font-size:13px;font-weight:600;cursor:pointer;}
  .su-wrap #suSketch canvas{display:block;border-radius:8px;max-width:100%;}
  .su-wrap #suEq{font-size:14px;margin:8px 2px 0;line-height:1.5;}
  .su-wrap #suEq b{color:#1f47c8;}
</style>
<div class="su-wrap sim">
  <div class="su-row">
    <button id="suPlay">⏸ Pausar</button>
    <div class="sld"><label>Ângulo de ataque α = <span class="val" id="suAv">8</span>°</label>
      <input type="range" id="suA" min="-4" max="22" value="8" step="1"></div>
    <div class="sld"><label>Velocidade v = <span class="val" id="suVv">8</span> m/s</label>
      <input type="range" id="suV" min="3" max="12" value="8" step="0.5"></div>
  </div>
  <div id="suSketch"></div>
  <div id="suEq"></div>
</div>
<script>
(function(){
  const el=id=>document.getElementById(id);
  let t=0, playing=true;
  const cw=()=>{ const w=(el('suSketch').clientWidth)||960; return Math.max(340,Math.min(1000,w)); };
  const rho=1025, A=12;
  const CL=al=>al<=16?0.11*al:Math.max(0.3,1.76-0.14*(al-16));
  const CD=al=>0.02+0.006*al*al/16 + (al>16?0.08*(al-16):0);
  new p5((p)=>{
    p.setup=()=>{ p.createCanvas(cw(),380).parent('suSketch'); };
    p.windowResized=()=>{ p.resizeCanvas(cw(),380); };
    p.draw=()=>{
      const W=p.width;
      const al=+el('suA').value, v=+el('suV').value;
      el('suAv').textContent=al; el('suVv').textContent=v.toFixed(1);
      if(playing) t+=p.deltaTime/1000;
      const cl=CL(al), cd=CD(al), stall=al>16;
      p.background('#eef2f6');
      // ── esquerda: perfil + escoamento ──
      const cx=W*0.26, cy=180, ch=120, rad=-al*Math.PI/180;
      // linhas de corrente (adere se não estol; descola se estol)
      for(let k=-3;k<=3;k++){ const y0=cy+k*22;
        p.push(); p.noFill(); p.stroke(31,71,200,150); p.strokeWeight(1.3); p.beginShape();
        for(let g=cx-ch;g<=cx+ch*1.4;g+=6){ const dx=g-cx;
          let dy=0; const near=Math.exp(-Math.pow(dx/(ch*0.7),2));
          if(k===0) dy=0; else dy=(k>0? -1:1)*near*(k>0? (stall?0:1):1)*8*Math.sign(k);
          // desvio pelo perfil: acima acelera (sobe), abaixo desce
          const defl = -Math.sign(k)*near*Math.abs(al)*0.8;
          let yy=y0+defl;
          if(stall && k>0 && dx>0) yy=y0 - near*30 + 12*Math.sin(dx*0.3 + t*6);  // esteira turbulenta
          p.vertex(g,yy);
        } p.endShape(); p.pop(); }
      // perfil (elipse achatada) inclinado
      p.push(); p.translate(cx,cy); p.rotate(rad);
      p.noStroke(); p.fill('#55524c'); p.ellipse(0,0,ch*1.3,ch*0.24);
      p.pop();
      // setas de pressão: sucção (cima) e pressão (baixo)
      p.stroke('#c81f1f'); p.strokeWeight(2);
      p.line(cx,cy-30,cx,cy-30-cl*14); // sucção para cima (representa L)
      p.noStroke(); p.fill('#c81f1f'); p.triangle(cx,cy-30-cl*14-6,cx-4,cy-30-cl*14+2,cx+4,cy-30-cl*14+2);
      p.fill('#7a7367'); p.textSize(11); p.textAlign(p.LEFT,p.CENTER);
      p.text('L',cx+6,cy-30-cl*7);
      p.textAlign(p.CENTER,p.TOP); p.text(stall?'ESTOL — escoamento descolado':'escoamento aderente',cx,cy+90);
      // seta do escoamento incidente
      p.stroke('#8a8073'); p.strokeWeight(1.5); p.line(cx-ch-24,cy,cx-ch-6,cy);
      p.noStroke(); p.fill('#8a8073'); p.triangle(cx-ch-6,cy,cx-ch-12,cy-4,cx-ch-12,cy+4);
      p.textAlign(p.CENTER,p.BOTTOM); p.text('v',cx-ch-15,cy-4);

      // ── direita: curva C_L(α) ──
      const gx0=W*0.56, gx1=W-16, gy0=40, gy1=330;
      const X=aa=>gx0+(aa+4)/26*(gx1-gx0), Y=c=>gy1-c/2*(gy1-gy0);
      p.stroke('#d8cfc0'); p.line(gx0,gy1,gx1,gy1); p.line(gx0,gy0,gx0,gy1);
      p.noStroke(); p.fill('#8a8073'); p.textSize(9); p.textAlign(p.CENTER,p.TOP);
      for(let aa=0;aa<=20;aa+=5) p.text(aa+'°',X(aa),gy1+4);
      p.textAlign(p.LEFT,p.TOP); p.text('C_L',gx0+4,gy0);
      p.push(); p.noFill(); p.stroke('#1f47c8'); p.strokeWeight(2.2); p.beginShape();
      for(let aa=-4;aa<=22;aa+=0.5) p.vertex(X(aa),Y(CL(aa))); p.endShape(); p.pop();
      p.stroke('#c81f1f'); p.strokeWeight(1.2); p.drawingContext.setLineDash([5,4]);
      p.line(X(16),gy0,X(16),gy1); p.drawingContext.setLineDash([]);
      p.noStroke(); p.fill('#66308c'); p.circle(X(al),Y(cl),9);

      const q=0.5*rho*v*v*A, L=q*cl, D=q*cd;
      el('suEq').innerHTML='C<sub>L</sub> = <b>'+cl.toFixed(2)+'</b>, C<sub>D</sub> = '+cd.toFixed(3)+
        ' &nbsp;|&nbsp; L = ½ρv²A·C<sub>L</sub> = <b>'+(L/1e3).toFixed(0)+' kN</b>, D = '+(D/1e3).toFixed(1)+' kN'+
        ' &nbsp;|&nbsp; L/D = '+(cl/cd).toFixed(0)+(stall?' — ESTOL':'');
    };
  });
  el('suPlay').onclick=()=>{ playing=!playing; el('suPlay').textContent=playing?'⏸ Pausar':'▶ Reproduzir'; };
})();
</script>
```

::: {.pergunta}
1. Aumente $\alpha$ de 0 a 15°: $C_L$ cresce como? E ao passar de 16°, o que acontece
   com a sustentação e com o escoamento no dorso?
2. Dobre a velocidade: a sustentação dobra ou quadruplica? Confirme por $L \propto v^2$
   — a mesma razão de o leme "pegar" melhor com mais máquina.
3. O ponto de melhor $L/D$ fica em ângulo baixo ou alto? Por que uma hélice eficiente
   opera longe do estol?
:::

## Onde o fenômeno vive

<!-- Seção incremental (AULA-TEMPLATE §4): exemplos podem ser promovidos a simulação
     em sessões futuras, a pedido do professor. -->

- **Leme** — uma asa vertical: força ∝ v²; por isso o navio "perde o leme" a baixa
  velocidade e por isso dá-se máquina adiante ao girar — o jato do hélice restaura o
  v² sobre o leme.
- **Hélice** — cada pá é uma asa em rotação; o empuxo é a soma das sustentações das
  seções da pá.
- **Estabilizadores de aleta** — asas retráteis no bojo que geram momento contra o
  roll, comandadas em fase pela central de estabilização.

<!-- EXEMPLO A SIMULAR: força de leme × ângulo de ataque (p5.js): curva com estol e
     slider de velocidade mostrando a dependência em v². -->

**Conexão com o próximo tópico:** calculamos sustentação usando coeficientes empíricos $C_L$. Mas existe uma **teoria** que fundamenta essa sustentação sem usar dados experimentais — o **Teorema de Kutta-Joukowski** — que liga sustentação à **circulação** ao redor da pá. Tópico 3.8, o último desta unidade.
